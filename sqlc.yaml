version: "2"
sql:
  - engine: postgresql
    queries: queries
    schema: internal/db/migrations
    gen:
      go:
        package: repository
        out: internal/repository
        sql_package: pgx/v5
        emit_prepared_queries: false
        emit_interface: true
        emit_json_tags: false
        emit_db_tags: false

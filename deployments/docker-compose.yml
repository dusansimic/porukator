name: porukator

services:
  postgres:
    image: postgres:17-alpine
    container_name: porukator_postgres
    hostname: postgres
    environment:
      POSTGRES_USER: porukator
      POSTGRES_PASSWORD: porukator
      POSTGRES_DB: porukator
    volumes:
      - postgres-data:/var/lib/postgresql/data
    ports:
      # Published on 55432 to avoid clashing with other local Postgres (5432).
      # In-container service-to-service traffic still uses 5432.
      - "55432:5432"
    networks:
      - porukator_internal
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U porukator -d porukator"]
      interval: 10s
      timeout: 5s
      retries: 5
    restart: unless-stopped

  porukator:
    build:
      context: ..
      dockerfile: deployments/Dockerfile.server
    container_name: porukator
    hostname: porukator
    environment:
      PORUKATOR_HTTP_ADDR: ":8080"
      PORUKATOR_HTTP_PUBLIC_HOST: "http://localhost:8080"
      PORUKATOR_POSTGRES_URL: "postgres://porukator:porukator@postgres:5432/porukator?sslmode=disable"
      PORUKATOR_AUTH_MASTER_PASSWORD: "change-me"
      PORUKATOR_LOGGING_LEVEL: "info"
      PORUKATOR_LOGGING_FORMAT: "json"
      PORUKATOR_APP_ENV: "production"
    ports:
      - "8080:8080"
    depends_on:
      postgres:
        condition: service_healthy
    networks:
      - porukator_internal
    restart: unless-stopped
    logging:
      driver: "json-file"
      options:
        max-size: "10m"
        max-file: "3"

  # Static SPA only — an external reverse proxy routes /porukator.v1.* to the
  # backend, so the web UI has no backend dependency of its own.
  webui:
    build:
      context: ..
      dockerfile: deployments/Dockerfile.webui
    container_name: porukator_webui
    hostname: webui
    ports:
      - "8090:80"
    restart: unless-stopped

volumes:
  postgres-data:

networks:
  porukator_internal:
